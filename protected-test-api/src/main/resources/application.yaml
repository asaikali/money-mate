spring:
  application:
    name: protected-test-api
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000
          jwk-set-uri: http://localhost:9000/oauth2/jwks

server:
  port: 8082

identity-broker:
  issuer-uri: http://localhost:9000
