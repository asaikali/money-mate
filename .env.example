# Example environment file for OBP sandbox setup
# Copy this to .secrets/.env and fill in your actual credentials

# OBP API Credentials
OBP_USERNAME=admin@example.com
OBP_PASSWORD=your-password-here
OBP_CONSUMER_KEY=your-consumer-key-here

# Path to your sandbox data import file
OBP_IMPORT_FILE=/Users/adib/dev/OpenBankProject/OBP-API/obp-api/src/main/scala/code/api/sandbox/example_data/example_import.json

# Optional: Override API base URL (defaults to http://localhost:8081)
# OBP_HOST=http://localhost:8081

# Optional: Override API version (defaults to v5.0.0)
# OBP_API_VERSION=v5.0.0

# Optional: Sandbox data import secret (defaults to mysecret)
# OBP_SANDBOX_DATA_IMPORT_SECRET=mysecret

# Spring Boot Profiles
# For Spring Boot applications, you can also use profile-specific YAML files:
#   - application-local.yaml (local dev credentials)
#   - application-public-sandbox.yaml (public sandbox credentials)
# These files are git-ignored and should be created in src/main/resources/
# See application-*.yaml.example files for templates
